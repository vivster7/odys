// Scratch page to experiment with React Components
import React from 'react';
import Input from '../components/Input';

const Scratch: React.FC = () => {
  return <Input x={100} y={100} text={'hello friend'}></Input>;
};

export default Scratch;
