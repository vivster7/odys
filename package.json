{
  "name": "odys",
  "version": "1.0.0",
  "private": true,
  "description": "diagrams, diagrams, diagrams",
  "dependencies": {
    "apollo-client": "^2.6.8",
    "knex": "^0.21.1",
    "knex-migrate": "^1.7.4"
  },
  "devDependencies": {
    "@graphile-contrib/pg-simplify-inflector": "^6.0.0",
    "husky": "^4.2.5",
    "lint-staged": "^10.2.2",
    "prettier": "^2.0.5"
  },
  "scripts": {
    "start": "cd client && npm start",
    "build": "cd client && npm build",
    "test": "cd client && npm test"
  },
  "license": "MIT",
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "client/src/**/*.{js,jsx,ts,tsx,json,css,scss,md}": [
      "prettier --single-quote --write",
      "git add"
    ]
  }
}